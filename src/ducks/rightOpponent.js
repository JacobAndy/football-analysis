import axios from "axios";
const initialState = {
  currentTeam: 1,
  currentYear: null
};

//action creators

//reducer
export default function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

//functions
